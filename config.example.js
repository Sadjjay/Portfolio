// Template configuration file.
//
// Setup:
//   1. Copy this file to "config.js" (same folder)
//   2. Paste your YouTube Data API v3 key inside the quotes below
//   3. config.js is gitignored, so it never gets committed
//
// To get a key, see comments in script.js (loadYouTubeStats section).

window.PORTFOLIO_CONFIG = {
    YOUTUBE_API_KEY: ''
};
